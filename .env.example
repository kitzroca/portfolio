GITHUB_USERNAME=kitzroca
GITHUB_TOKEN=your-github-token-here

# Cloud Database Backend (Supabase) for Live Peer Endorsements / Vouches
# 1. Create a free project at https://supabase.com
# 2. Run this SQL in Supabase SQL Editor:
#    create table vouches (
#      id text primary key,
#      name text not null,
#      role text not null,
#      company_or_institution text not null,
#      quote text not null,
#      initials text not null,
#      created_at timestamp with time zone default timezone('utc'::text, now()) not null
#    );
#    alter table vouches enable row level security;
#    create policy "Public read" on vouches for select using (true);
#    create policy "Public insert" on vouches for insert with check (true);
#    create policy "Public delete" on vouches for delete using (true);

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key